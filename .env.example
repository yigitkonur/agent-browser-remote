API_TOKEN=          # Required. Generate with: openssl rand -hex 32
AGENT_BROWSER_ENCRYPTION_KEY=   # Optional. AES-256-GCM key for session state encryption: openssl rand -hex 32
MAX_SESSIONS=10
STATE_EXPIRE_DAYS=30
